# v22.x 2단계 개발·검사 보고서

갱신: 2026-08-31. 상태: **DEVELOPMENT CANDIDATE / NOT PUBLIC**. 1단계 출시 관문을 낮추거나 대신하지 않는다. 결제 S2-007은 범위에서 제외한다.

## 현재 카드

| 카드 | 구현 | 자동·브라우저 | 실제 Android·사용자·운영 | 판정 |
|---|---|---|---|---|
| S2-001 빠른 캡처 | 홈 바로가기, POST 공유 대상, 1회성 임시 보관함, 미리보기, 명시적 저장, 중복 방지 | PASS | 실제 Android 공유 시트·오프라인 PWA 대기 | IN-PROGRESS · DONE-A+B |
| S2-002 개인 시작 화면 | 기존·빠른 기록·이관·연결 중심, 최근 작업, 명시적 초기화 | 선택·재열기·손상 설정·기존 사용자 불변 PASS | 2단계 실제 사용자 재검증 대기 | DONE-A+B |
| S2-003 검색 일치 이유·저장 필터 | 분야별 일치 이유, 안전 강조, 저장·실행·삭제 UI | 한글·특수문자·휴지통·첨부명·내부연결·1만 노트 PASS | 2단계 실제 사용자 재검증 대기 | DONE-A+B |
| S2-004 연결 재발견 | 들어오는·나가는·깨진·연결 없는 노트 보기 | 제목 변경·삭제·복원·원문 불변·1만 노트 PASS | 2단계 실제 사용자 재검증 대기 | DONE-A+B |
| S2-005 성능 예산 차단 | 성능 상태 화면, P95 예산, CI 실패 조건 | 3배 CPU·1만 노트·반복 저장·다중 탭·1GiB 계약 PASS | 실제 Android 저사양 재검증 대기 | DONE-A+B |
| S2-006 선택형 최소 측정 | 기본 끔, 7개 허용 필드, 미리보기, 거부·삭제 | 민감 표본 0·기능 차이 0·손상 설정 안전 PASS | 수신점·보존기간·운영자 승인 대기, 현재 전송 0 | DONE-A+B / WAITING-O |
| S2-007 결제 | 제외 | NOT_RUN | 사업자·가격 미정 | OUT-OF-SCOPE |
| S2-008 출시 후보 | 미공개 | NOT_RUN | 1단계 관문 대기 | BLOCKED |

## S2-001 안전 계약

- 공유 내용은 GET 주소에 넣지 않는다.
- 서비스워커의 별도 임시 보관함을 사용하며 앱이 읽으면 즉시 삭제한다.
- 새 공유가 들어올 때 24시간을 넘긴 임시 항목을 정리한다.
- 제목 300자, 본문 20,000자, HTTP(S) URL 2,048자로 제한한다.
- HTML은 텍스트로만 미리보기·저장하며 실행하지 않는다.
- 사용자가 저장을 누르기 전 노트 상태는 변하지 않는다.
- 같은 내용이 10분 안에 반복 전달되면 기존 노트를 열고 중복 생성하지 않는다.

## S2-002 안전 계약

- 사용자가 직접 선택하기 전 기존 노트 화면을 바꾸지 않는다.
- 시작 방식은 현재 기기의 별도 로컬 설정에만 저장하며 Google·Drive·노트 상태에 넣지 않는다.
- 손상되거나 알 수 없는 설정은 기존 화면으로 되돌린다.
- 초기화는 시작 화면 설정만 지우고 노트·첨부·계정 설정을 바꾸지 않는다.
- 최근 작업 제목은 DOM 텍스트로만 표시한다.

## S2-003 안전 계약

- 검색어는 정규식이나 HTML로 실행하지 않고 일반 문자열로 비교한다.
- 강조 표시는 `textContent`와 DOM `mark`만 사용한다.
- 제목·본문·태그·노트북·첨부 이름·내부 연결의 일치 이유를 구분한다.
- 활성 노트와 휴지통 검색 결과를 자동으로 섞지 않는다.
- 저장 필터는 최대 20개·이름 60자·조건 160자로 제한한다.

## S2-004 안전 계약

- 연결 보기는 현재 노트 원문을 읽기만 하며 링크를 자동 수정하지 않는다.
- 활성 노트에 없는 대상은 깨진 연결로 분리한다.
- 제목 변경·휴지통 이동·복원 뒤에는 저장된 오래된 목록이 아니라 현재 상태에서 다시 계산한다.
- 같은 대상의 반복 링크는 인덱스에서 중복 계산하지 않는다.
- 목록에는 최대 100개를 표시하되 전체 수량은 전수 계산한다.

## S2-005 성능 계약

- CI의 3배 CPU 모사·1만 노트에서 검색 P95 108ms, 열기 P95 26ms, 반복 저장 P95 318ms, 연결 인덱스 27ms였다.
- 예산은 검색 500ms, 열기 300ms, 저장 500ms, 한 작업 2,000ms이며 하나라도 넘으면 검사가 실패한다.
- 검색용 정규화 색인은 노트 객체와 내용 서명을 함께 확인하므로 제목·본문·태그·첨부 이름이 바뀌면 다시 계산한다.
- 성능 상태 화면의 측정값은 현재 탭 메모리에만 두며 노트 원문이나 검색어를 기록하지 않는다.
- 1GiB 첨부 스트림과 1만 노트 라이브러리는 기존 독립 검사도 계속 전체 검사에 포함한다.

## S2-006 개인정보 계약

- 기본값은 꺼짐이며 사용자가 현재 기기에서 직접 켜야 한다.
- 허용 필드는 `schema`, `event`, `stage`, `status`, `errorCode`, `durationBucket`, `countBucket` 7개뿐이다.
- 노트 원문·검색어·첨부 이름·전체 URL·열쇠·위치를 넣은 공격 표본에서도 저장된 민감 표본은 0개였다.
- 끄면 로컬 기록을 지우고, 별도 삭제 버튼으로 언제든 기록만 지울 수 있다. 두 경우 모두 노트 상태 지문은 그대로였다.
- 현재 수신점이 없으므로 전송 명령과 네트워크 API는 모듈에 없다. 운영자 계약 전까지 로컬 미리보기만 제공한다.
- 세부 기술 계약은 `PRIVACY_TELEMETRY_V22.md`에 고정했다.

## 남은 실제 증거

1. 설치된 Android PWA의 공유 시트에서 노트플러스P 선택.
2. 텍스트·URL·빈 입력·거대 입력·악성 HTML·반복 전달.
3. 오프라인 공유 후 재열기와 영구 저장 확인.
4. 시험 자료 제거와 원래 기기 상태 복구.

## 2026-08-31 최종 자동검사와 공개 판정

- 빠른 검사: **21/21 PASS**.
- 전체 회귀검사: **68/68 PASS**. 1GiB 스트림, 1만 노트, 데이터 보존, 접근성, v16~v21 복귀, S2-001~006을 포함한다.
- 전체 검사 중 S2-005 실제 수치: 3배 CPU에서 검색 P95 46ms, 열기 14ms, 저장 126ms, 연결 인덱스 17ms. 실행마다 달라질 수 있으므로 위 안전 계약의 고정 기록과 최신 전체 실행을 구분한다.
- 출시 게이트: **허용 안 됨**, 차단 11개. 운영자 실제 값 7개, 실제 PC↔Android Drive, TalkBack, 비개발자 3명, 운영 훈련이 남았다.
- 교차기기 사전점검: **BLOCKED / 데이터 쓰기 0**. HTTPS URL 2개, CDP 끝점 2개, Android 기기, OAuth 프로젝트 접근이 없다.
- Android 사전점검: ADB 온라인 0대, `SKIP_NO_DEVICE`.
- 결론: 자동·브라우저로 닫을 수 있는 S2-001~006은 완료했으나 1·2단계 전체와 공개 상용화 100%는 아니다. 현재 공개판은 v21을 유지한다.
- 첫 원격 Actions `33394083599`는 제품 단언 실패가 아니라 GitHub Windows 실행기에 브라우저 시험 드라이버가 없어 6개 검사가 시작 전 `MODULE_NOT_FOUND`로 끝났다. 브라우저 검사를 빼지 않고 로컬과 같은 `playwright-core` 1.62.1을 CI에 명시적으로 설치하도록 보완한 뒤 재실행한다.
