# v22 1단계 개발·검사 보고서

갱신: 2026-08-26. 상태: DEVELOPMENT CANDIDATE / NOT COMMERCIAL RELEASE. 목표는 무료 상용판이며 결제·자동 유료 전환은 없다. 99% 달성을 선언하지 않는다.

## 구현 결과

- 기존 v21 제품 파일을 보존한 별도 v22 core·loader·Drive·manifest·worker.
- 새 노트/이관/건너뛰기 시작 안내, 기존 사용자 자동 안내 방지, 저장 제한 안내.
- 로컬 저장·Drive 상태·실패 이유·다음 행동·완료 백업 시각·진행/충돌 수.
- 복원 전 변화량과 명시적 선택, 양쪽 보존 금고, 개별 보존본 적용, 동시 편집 덮어쓰기 차단.
- 최근 이관 기록과 실제 노트·첨부 수량/바이트/지문 대조, 내용 없는 JSON 보고서.
- 전체 Markdown·첨부·metadata ZIP, 새 빈 보관함 왕복, 휴지통·복원점·금고 첨부 보존, 경로/중복/손상 차단.
- 1만 노트 목록 100개씩 페이지 표시. 검색 대상은 전체 노트. 최대 2MiB 입력 조각, 거대 단일 노트 안전 제한.
- 대화창 초점·입력 이름·동작 감소·360~1920px 화면 보완.
- 무료 정책·자료 권리·개인정보·약관·지원·상태 초안 6개, 운영/장애/사용자 시험 절차, 실제 출시 차단 검사.
- 저장 방식·영구 보관 권한·사용량/한도·최근 전체 반출·다른 기기 Drive 사본을 구분한 저장 건강 진단. 로컬 기본과 사용자가 선택한 Drive appData 이동, 다른 사람/AI 제공자 자동 전송 없음, 무료 반출 고지를 앱과 문서에서 일치시켰다.

## 검사 결과와 증거 범위

1. 최초 저장 기록 `4353f36`에서 전체 50/50 PASS. 이후 대형 검사로 발견한 문제를 수정했으므로 이 숫자를 최종 코드 검증으로 재사용하지 않는다.
2. `4f82e91` 전체 53/53 PASS 이후 저장소 분리·상태·보고서 보완을 추가했다. 제품 코드 `f84d7a0`과 검사 실행기 수정 `b5e869a`에서 빠른 9/9와 전체 54/54가 PASS했고, 저장 건강·자료 흐름·실기기/혼합 대형 증거를 포함한 `3cd0603`에서 전체 56/56를 PASS했다. 암호화 전체 백업 왕복을 추가한 `893c5c8`에서 전체 57/57을 PASS했고, 출시 증거 감사기를 추가한 `7444967`에서 전체 58/58을 다시 PASS했다. 최신 전체 집계는 `FULL_RESULT tests=58 failed=0`이다.
3. Edge·Chrome: 390px/1280px 첫 기록·저장·재열기 PASS. 양쪽 360·390·640·1280·1920px, 이름/중복 ID/가로 넘침/상태 창 초점 순환 PASS. 자동 보조 검사이며 전체 WCAG·TalkBack 인증 아님.
4. 합성 자료 1,000종의 안전 필드 보존, 50회 실제 저장, 시작 안내 조건 1,000종 PASS. 실제 사람 1,000명의 성공률이나 1,000개 독립 브라우저 프로필이 아님.
5. 100개 충돌 양쪽 보존, 명시적 승인/취소, 개별 보존본 적용, 새 편집 덮어쓰기 차단 PASS. 충돌 금고 전용 첨부와 ID 충돌/손상 다운로드도 별도 검사한다.
6. 1GiB XML 읽기: 1,075,089,627바이트·1만 노트, 최대 조각 2,097,152바이트, 마지막 측정 약 10초·응답 간격 61ms, 취소 뒤 reader 0. 대부분 공백인 입력이며 첨부 저장 증거가 아니다.
7. 첨부가 있는 1GiB 영구 저장: ENEX 16개를 한 개씩 순서대로 이관. 원본 합계 1,433,850,522바이트, 신규 10,000노트, 첨부 1,024개·1,073,741,824바이트. 약 100,148ms, 최대 관찰 간격 778ms. 재열기 후 첨부 전수 SHA-256 일치, 누락 0, 기존 1노트 동일, 저장 제한 false. Edge 일반 전용 프로필이며 Android·여러 형식·16개 동시 선택 성능의 증거는 아니다.
8. 32MiB 첨부 분석, ZIP+PDF 휴지통 왕복, 악성 HTML 50종, ZIP 중복·경로·누락·손상 및 거대 XML 차단 PASS.
9. 실제 Galaxy S21+에서 USB 역방향 연결과 Chrome 개발 연결로 v22 후보를 검사했다. 411px 화면 가로 넘침 0, IndexedDB 저장·재열기, ENEX 노트+69바이트 PDF 재열기, 서비스워커 활성/제어, 오프라인 재실행, 모바일 설치 경로가 PASS했다. 시험 합성 노트·첨부는 0개로 정리됐고 시작 상태 서명과 원래 탭을 복구했다. 개인정보 보호를 위해 화면은 보관하지 않았다. 증거는 `artifacts/physical_android_alpha_v22.json`; 실제 Drive 왕복과 TalkBack을 포함하지 않는다.
10. 대표 혼합 대형 자료는 개인정보 없는 결정적 합성 자료로 16개 ENEX·신규 10,000노트·첨부 1,024개·정확히 1GiB를 영구 저장하고 재열기했다. PDF, JPEG 방향정보 표식, PNG, MP3, Office 모양, 일반 바이너리 6종과 한글 제목/태그/날짜/내부 연결/중복 파일명을 포함한다. 첨부 누락·크기/SHA-256 불일치·중복 노트 ID는 모두 0, reader 0, 기존 상태 불변, 최대 관찰 간격 1,227ms였다. 첫 실행의 JPEG/PNG 기대 지문 생성 오류 342건은 FAIL로 보존하고 Uint8Array 기준으로 고쳐 전체를 다시 실행한 결과다. 증거는 `artifacts/v22_mixed_large_library_v1.json`; 실제 개인 자료나 Android 성능 증거는 아니다.
11. 저장 건강 전용 검사는 IndexedDB/제한 저장, 영구 보관 허용/미허용, 용량 65%/85% 경계, 한도 미확인, 전체 반출 기록 없음/정상/미래 조작을 확인했다. 자료 흐름 검사는 앱·Drive 설정·개인정보·자료 권리 문서의 로컬/Google/다른 사람/AI/무료 반출 문구가 일치함을 확인했다.
12. 암호화 전체 백업은 합성 비밀번호로 실제 복호화 왕복, 오답 비밀번호·잘못된 형식 거부, 암호문 원문 비노출, 전체 반출 기록 재열기, 원본 노트 상태 불변을 확인했다.
13. 출시 증거 감사기는 후보판·방법·날짜·안전한 상대 경로·JSON 증거 메타데이터·저장 기록을 확인했다. 현재 3개 PASS 증거는 유효하고 Drive/TalkBack/3명/운영 훈련 4개는 정상적으로 NOT_RUN이며, 출시 허가는 아니다.

검사 구간 구분: 전체 53/53은 저장소 분리 전 `4f82e91`, 전체 54/54는 저장 건강 변경 전 `b5e869a`, 전체 56/56은 암호화 왕복 검사 추가 전 `3cd0603` 증거다. 최신 `7444967`은 전체 58/58 결과를 사용한다. 실행 정책은 시스템 전체를 변경하지 않고 별도 PowerShell 프로세스에만 허용해 공통 실행기를 검증했다.

### 이전 버전 복귀 안전성 — 저장소 분리

- 재현한 위험: 공유 DB에서 v21 첨부 정리가 v22 충돌 금고 전용 첨부를 참조하지 않았다. 실제 정리를 실행해 사용자 데이터를 지우지 않고, 합성 프로필의 참조 판정으로 위험을 확인했다.
- 수정: `noteplusp_v22_schema5` 및 전용 보조 키. 이전 DB 단일 읽기 전용 복사, 첨부 참조/크기/SHA/공간 확인, 새 DB 한 작업으로 확정. 이전 DB/보조 키 삭제·덮어쓰기 없음. 이미 복사된 v22 상태는 재복사하지 않는다.
- 실패 안전: 작은 표본에서 손상·용량 부족은 새 root/보조 키를 쓰지 않고 안전 안내로 편집 진입 차단. 원본 상태·첨부가 그대로임을 확인했다.
- 교차 버전: 원본 복사 동일, v22 독립 편집, v21 정리의 v22 영향 없음, v21 추가 편집이 v22를 덮어쓰지 않음, 재열기 PASS.
- 대형 복사: 합성 10,000노트·첨부 1,024개·1,073,741,824바이트를 이전 DB에서 v22로 복사, 전수 지문 불일치 0, 원본 상태 지문 동일, 이후 독립 편집/재열기 PASS. 소요 63,436ms(검증·왕복 포함). 로그 `large-legacy-copy.log`. PC Edge·반복 바이너리이며 실제 Android/혼합 미디어 시험이 아니다.
- 사용자 의미: 초기 복사 이후 v21/v22의 편집은 자동으로 서로 반영되지 않는다. v21 복귀는 최신 v22 편집의 역방향 동기화가 아니다. v22 전체 ZIP을 별도로 보관한다.
- 상태 표시 보완: 완료 전 이벤트·잘못된 백업 ID·미래/역행 시각·음수/NaN 대기 수 차단. 정상 완료 문구의 노트 수 401/503을 오류 코드로 오해하지 않는다. 로컬 저장 실패를 준비 완료로 표시하지 않는다.
- 공유용 이관 보고서 보완: 원문 파일명·노트북명·보고서 ID 제외, 실제 첨부 바이트와 수량·보고서 지문값만 출력. 잘못된 합계·누락·해시 없음·되돌린 이관·저장 제한 검사 PASS.

## 발견·수정·미확인 기록

- 최초 대형 시험: 마지막 64개 첨부 확정 전에 일반 저장 후 재열기하여 960/1,024개만 확인됐다. 자동 저장도 같은 중간 상태를 저장할 수 있었다. 첨부 지문 계산+노트/첨부 동시 저장이 완료되기 전 일반 저장을 대기시키도록 수정했다. 지연된 첨부 작업을 일부러 넣는 전용 검사가 이전 저장본 보존과 최종 PDF 재열기를 확인한다.
- 두 번째 대형 시험: 임시 프로필에서 `partial` 결과. 실패 기록을 보존한다. 당시 상세 저장소 원인이 충분히 기록되지 않아 원인을 확정하지 않는다.
- 세 번째 시험: 일반 전용 프로필로 전환했으나 실행 세션이 사라지고 6묶음 이후 최종 결과 없음. `INTERRUPTED`, PASS가 아니다.
- 네 번째 시험: 파일을 한 개씩 생성/이관해 입력 보관 메모리를 낮추고, 영구 저장된 첨부의 임시 메모리 참조를 해제한 뒤 전수 PASS. 한꺼번에 큰 다중 파일을 선택하는 경로의 모든 한계가 해결됐다고 주장하지 않는다.
- 시작 안내 자동 시험에서는 비동기 완료 전에 다음 입력을 넣는 시험 순서를 발견했다. 화면 닫힘·처리 종료를 기다리도록 고쳤고 실제 이중 선택도 막았다.
- Drive의 v22 완료 포인터를 이전 버전과 분리했다. v22 포인터가 없을 때만 이전 백업을 읽으므로 v21의 마지막 완료 백업을 덮어쓰지 않는다. 충돌 금고 첨부도 백업 목록에 포함한다.

## 출시까지 남은 관문

- 실제 승인된 시험 출처에서 PC→Android→PC Drive 왕복 및 원상 복구.
- 실제 Android의 기본 저장·재열기·오프라인·설치 경로는 PASS. TalkBack 핵심 과업은 별도 미완료.
- 연결된 Galaxy에는 Samsung TalkBack이 설치됐으나 접근성 서비스와 터치 탐색이 비활성 상태였다. 읽기 전용 확인이며 실제 TalkBack 과업 PASS가 아니다.
- 운영자 6항목: 운영 주체, 지원 연락, 개인정보 문의, 관할/이용조건 검토, 응답 목표, 장애 책임자. 현재 null/미검토다.
- 실제 비개발자 3명의 핵심 과업과 실제 운영자 관찰 훈련.
- 혼합 형식 1GiB·여러 ENEX 순차 처리 자료는 PASS. 여러 파일을 한 선택으로 넣는 1GiB 성능, 전체 대화창 대비·보조 기술 전수 검토는 별도 범위다.
- S1-003/005/006/009 경계 조건과 증거를 최신 전체 검사로 대조해 DONE-A+B로 닫았다. 현재 완료 카드는 S1-001/002/003/005/006/007/009의 7개이며 총 12개 중 7개다. S1-004는 Android 기본만 PASS이고 Drive 왕복이 남았다. 카드는 크기가 다르므로 7/12를 제품 완성률로 환산하지 않는다.

`node tools/check_release_gate_v22.mjs`는 실제 입력이 미완료면 `releaseAllowed:false`와 비정상 종료를 반환한다. 긍정 합성 시험이 통과했다고 실제 출시 허가가 생기는 것은 아니다. 참조 보고서 내용은 담당자가 확인해야 한다.

## 재현 명령

```powershell
node tools/check_plan_v11.mjs
node tests/test_v22_release_contract_v1.mjs
node tests/test_v22_release_gate_v1.mjs
# 전체 검사는 저장 기록을 만든 깨끗한 작업 사본에서 실행
# PowerShell 파일 실행이 허용된 환경에서만 기존 실행기를 사용한다.
# 현재 환경에서는 실행 정책을 바꾸지 않고 각 tests/*.mjs를 Node로 실행한다.
# 대형 시험은 여유 메모리 3GiB·디스크 5GiB 확인 후 별도 실행
$env:NOTEPLUS_LARGE_E2E='1'
node tests/test_v22_large_durable_import_v1.mjs
node tests/test_v22_large_legacy_copy_v1.mjs
# 차단됨이 현재 정상 결과. 실제 출시 승인과 구별
node tools/check_release_gate_v22.mjs
```

큰 시험의 전용 프로필과 원시 로그는 프로젝트 루트의 `.project-continuity/local/v22-20260826/`에 보존한다. 공개 코드 보관소에는 개인 프로필·원시 기기 식별자·개인 노트·인증정보를 올리지 않는다. 이번 시험 프로필에는 합성 자료만 있다.

## 변경 반영

- 코드 보관소: `hanksleekorea-boop/noteplusp`, 실제 public. 인증 계정과 admin/push 확인.
- 개발 작업 갈래: `codex/v22-stage1`. main 직접 변경·강제 변경 없음.
- 공개 main 기준: `21bb9321be5da26412df564937ef56bed325c474`. v21·PC 기본 주소 유지.
- 원격 반영 상태: 제품 코드 `328d624`와 검사 기록 문서 `d6f0034`를 정상 gh 로그인으로 반영하고 각 저장 기록 및 tree의 로컬·원격 정확한 일치를 확인했다. [초안 PR 7](https://github.com/hanksleekorea-boop/noteplusp/pull/7) 생성. 이후 이 상태 문서는 별도 문서 저장 기록으로 추가한다. 상용 후보 병합·Pages 전환은 하지 않는다.
- 전송 경로: Git HTTPS DLL은 실행 정책 차단, 연결 앱은 쓰기 403이었다. 보안 정책을 변경하지 않고 기존 gh 로그인으로 [GitHub 공식 Git Data API](https://docs.github.com/en/rest/git/commits)를 사용했다. 기존 갈래를 강제 변경하지 않고 전체 파일 묶음 및 저장 기록 지문을 대조했다.
- 공개 상태: 2026-08-26 PC·v21·QR·worker 4/4 HTTP 200. 원격 main `21bb9321be5da26412df564937ef56bed325c474` 유지.
