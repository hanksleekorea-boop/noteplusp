# v22 실제 PC→Android 시험 절차

상태: PREPARED / 실제 Drive 왕복 미실행. 2026-08-26 Galaxy S21+ 개발 연결은 `device`까지 확인했으나 테스트용 USB 역방향 연결·Chrome 시작 명령이 실행 환경 정책으로 거부됐다. 거부된 명령을 다른 경로로 우회하지 않는다.

## 시작 전

1. 담당자는 v22 파일이 있는 승인된 시험 HTTPS 주소와 Google 로그인 허용 출처를 확인한다. 로컬 주소가 허용되는지 추측하지 않는다. 운영자나 허용 출처가 없으면 `BLOCKED`다. 상용 기본 주소를 바꾸어 시험하지 않는다.
2. 시험 전 양쪽 원본 보관함을 암호화 백업한다. 비밀번호·노트·첨부·계정 화면을 보고서나 공개 코드 보관소에 올리지 않는다.
3. 개인 노트가 없는 시험용 보관함/프로필을 사용한다. `pilot-assets/노트플러스P_파일럿_샘플.enex`만 이관한다. 노트 1개·PDF 1개·69바이트를 확인한다.
4. 원본이 있는 보관함을 초기화하지 않는다. 시험과 원본이 섞이면 중단하고 별도 프로필을 준비한다.

## 실행 및 성공 판정

1. PC에서 표본 가져오기 → 저장 완료 → 이관 대조 `일치` → 전체 JSON 내보내기.
2. PC에서 Google 로그인. 로그인만으로 전송되지 않는 것을 확인. `이 PC 데이터 백업`을 직접 선택하고 완료 백업 ID·시각을 기록한다.
3. Android에서 같은 시험 계정으로 로그인 → 클라우드 백업 확인. 로그인 자체는 자료를 바꾸지 않아야 한다.
4. 복원 미리보기의 노트·첨부 수와 백업 ID를 PC와 대조. 취소 한번 수행 후 노트 불변 확인. 다시 확인 후 명시적으로 복원한다.
5. 저장 완료 뒤 재열기. PDF가 열리고 노트·태그·날짜가 같아야 한다. Android 전체 JSON을 PC의 개인용 검증 폴더로 전송한다.
6. `node tools/compare_device_export_v22.mjs <PC JSON> <Android JSON>` 실행. 명령 인수는 실제 로컬 파일 경로다. `match:true`, 누락·크기 오류 0이어야 한다. 도구는 원문을 출력하지 않는다. 복원점·충돌 금고는 비교 대상에서 제외되므로 별도 육안 확인한다.
7. Android 비행기 모드 → 이미 준비된 앱 재열기 → 합성 노트 수정 → 저장 → 재열기. 오프라인 저장이 유지되어야 한다.
8. 인터넷 복구 → 사용자 선택 백업 → PC에서 확인·복원 → 수정 내용 대조. 자동 실시간 동기화로 오해하지 않아야 한다.
9. 이전 v21 직접 주소 복귀 시험. 개인 원본이 존재했던 시험이면 원본 복구 후 원래 지문값 대조. Drive 시험 파일은 자동 삭제하지 않는다.

## 기록할 것

- 날짜, 시험 담당자, 후보 저장 기록 SHA, OS/브라우저 버전, 시험 주소, PASS/FAIL 단계, 내용 없는 수량·지문값, 원상 복구 여부.
- 백업 ID·보고서에는 실계정 식별자와 원문을 넣지 않는다. 원자료 JSON은 공개 보관소에 넣지 않는다.
- 통과 뒤 `release-evidence-v22.json`의 해당 항목에 사실대로 결과·날짜·개인정보 없는 보고서 경로를 연결한다. 준비 문서를 실행 증거로 연결하지 않는다.

TalkBack 검사는 `QA_ACCESSIBILITY_V22_v1.md`를 따르며 자동 화면 폭 검사로 대체하지 않는다.
