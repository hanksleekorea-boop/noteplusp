# v22 선택형 품질 측정 개인정보 계약

갱신: 2026-08-31. 적용 대상: 미공개 v22 후보. 이 문서는 운영자 검토 전 기술 계약이며 법률 자문이나 공개 개인정보 처리방침 승인이 아니다.

## 기본 동작

- 기본값은 꺼짐이다.
- 사용자가 현재 기기에서 직접 켜야 기록을 시작한다.
- 현재 운영자 수신점이 없으므로 네트워크 전송 코드는 없고 모든 미리보기는 현재 기기 `localStorage`에만 남는다.
- 끄면 로컬 기록을 즉시 비우며 노트 편집·검색·백업·복원·내보내기 권리는 달라지지 않는다.

## 허용 필드

`schema`, `event`, `stage`, `status`, `errorCode`, `durationBucket`, `countBucket`만 허용한다. 시간과 개수는 원값이 아니라 고정 범위로만 기록한다. 최대 100개를 보관한다.

## 금지 데이터

노트 제목·본문, 검색어, 태그 이름, 노트북 이름, 첨부 이름·내용, 전체 URL, 이메일·계정 식별자, 인증 열쇠·토큰, 위치, IP 주소를 기록하지 않는다. 알 수 없는 입력 필드는 버린다.

## 운영자 결정 전 금지

수신점, 보존 기간, 삭제 처리, 접근 권한, 국외 이전, 문의 담당과 개인정보 처리방침 승인이 끝나기 전에는 전송 기능을 추가하거나 이 기능을 공개 수집으로 설명하지 않는다.
