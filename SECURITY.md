# 보안 정책

보안 문제에는 실제 사용자 데이터, 인증 문자열, 개인 식별정보를 포함하지 않는다. 공개 Issue에는 재현 가능한 최소 정보만 적고, 민감한 내용은 저장소 소유자가 지정한 비공개 경로가 마련되기 전에는 게시하지 않는다.

지원 대상은 현재 공개 후보와 기본 공개 버전이다. 과거 버전 문제는 현재 후보에서 재현되는지 먼저 확인한다.

보고에는 영향, 재현 조건, 예상 결과, 실제 결과를 포함하되 실제 노트·첨부·계정 정보는 제거한다.

Firebase Web API 키는 프로젝트를 식별하는 공개 클라이언트 설정이며 데이터 접근 권한 자체가 아니다. 그래도 Google Cloud Console에서 Firebase 관련 API만 허용하고 공개 앱 웹사이트만 허용해야 한다. Storage Rules와 App Check를 별도로 적용하며, 진단·검사·보고에는 키 원문을 다시 출력하지 않는다.
