# 실제 접근성 확인표

상태: 자동·키보드·화면 검사 PASS / TalkBack NOT_RUN.

Edge와 Chrome에서 360·390·640·1280·1920px, 글꼴 200%, 동작 감소 설정, 보이는 입력 이름, 중복 ID, 상태 창 초점 순환·Escape 복귀, 가로 넘침을 검사한다. `node tests/test_v22_accessibility_v1.mjs`. 이는 전체 WCAG 인증이 아니다.

Android의 TalkBack을 시험 담당자가 직접 켠 후 다음 과업을 수행한다. 기존 TalkBack 설정을 기록하고 끝나면 원래대로 되돌린다.

1. 시작 안내에서 ‘새 메모’ 선택, 제목·본문 입력, 저장 완료 음성 확인.
2. 노트 목록·검색·편집기로 이동하고 선택 노트를 연다.
3. 합성 ENEX 미리보기에서 선택/취소/확인 동작과 수량을 읽는다.
4. 저장 상태에서 로컬 저장과 Drive 완료 백업의 차이를 이해한다.
5. 복원 비교와 충돌 금고에서 현재/백업 보존본을 구분하고 취소한다.
6. 전체 내보내기 버튼 및 암호화되지 않은 ZIP 경고를 읽는다.
7. 모바일 탐색 버튼, 가로·세로 전환, 큰 글꼴, 브라우저 확대에서 핵심 버튼에 도달한다.

각 과업에 실제 읽힌 이름·초점 손실·오조작 여부·성공/실패만 기록한다. 개인 내용·계정 화면은 수집하지 않는다. 한 과업이라도 막히면 출시 차단 상태를 유지한다. 대비비와 모든 대화창 전수 감사는 별도 측정 결과가 없으면 미확인으로 기록한다.
